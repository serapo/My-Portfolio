import React from "react";
import Box from "@mui/material/Box";

function Main() {
  return (
    <Box
      sx={{
        textAlign: "center",

        padding: 0,
        margin: 0,
        height: "95vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "white",
      }}
    ></Box>
  );
}

export default Main;
